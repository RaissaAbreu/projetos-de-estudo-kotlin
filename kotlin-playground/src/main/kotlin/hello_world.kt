

fun main() {
    val originalText = "Hello World"
    println(originalText)
    println(originalText.uppercase())
    println(originalText.lowercase())
    println(originalText.reversed())
}